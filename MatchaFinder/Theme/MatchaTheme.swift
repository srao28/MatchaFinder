//
//  MatchaTheme.swift
//  MatchaFinder
//

import SwiftUI

enum MatchaTheme {
    /// Primary UI colour — same shade as `AccentColor` in the asset catalog.
    static let primary = Color.accentColor
    /// Secondary colour for screens, lists, and neutral surfaces.
    static let secondary = Color.white
}
